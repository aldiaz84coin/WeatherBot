{
  "name": "madrid-temp-bot",
  "private": true,
  "workspaces": [
    "packages/bot",
    "packages/dashboard"
  ],
  "scripts": {
    "bot": "pnpm --filter bot dev",
    "dashboard": "pnpm --filter dashboard dev",
    "backtest": "pnpm --filter bot backtest",
    "build": "pnpm --filter bot build && pnpm --filter dashboard build"
  }
}
